<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:statusBarColor">@color/colorBackgroundDark</item>
        <item name="android:navigationBarColor">@color/colorNavbar</item>
        <item name="android:navigationBarDividerColor">@color/colorNavbarDivider</item>
        <item name="android:windowBackground">@color/colorBackground</item>

        <item name="android:windowLightStatusBar">@bool/bool_light_system_bars</item>
        <item name="android:windowLightNavigationBar">@bool/bool_light_system_bars</item>
    </style>

</resources>
